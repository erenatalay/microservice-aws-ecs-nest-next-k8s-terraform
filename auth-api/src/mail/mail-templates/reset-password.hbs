<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=">
    <title>{{title}}</title>
    <style>
        .reset-code {
            font-size: 32px;
            font-weight: bold;
            color: #00838f;
            letter-spacing: 5px;
            background-color: #f2f2f2;
            padding: 15px 30px;
            margin: 20px auto;
            display: inline-block;
            border-radius: 8px;
            border: 1px dashed #00838f;
        }
        .button {
            display: inline-block;
            padding: 20px;
            background: #00838f;
            text-decoration: none;
            color: #ffffff;
            border-radius: 5px;
        }
        .expires {
            color: #a0a0a0;
            font-size: 14px;
            margin-top: 10px;
        }
    </style>
</head>

<body style="margin:0;font-family:arial">
    <table style="border:0;width:100%">
        <tr style="background:#eeeeee">
            <td style="padding:20px;color:#808080;text-align:center;font-size:40px;font-weight:600">
                {{app_name}}
            </td>
        </tr>
        <tr>
            <td style="padding:20px;color:#808080;font-size:16px;font-weight:100">
                {{text1}}<br>
                {{text2}}<br>
                {{text3}}
            </td>
        </tr>
        <tr>
            <td style="text-align:center">
                <a href="{{url}}" class="button">{{actionTitle}}</a>
            </td>
        </tr>
        <tr>
            <td style="text-align:center;padding:30px 20px;">
                <p style="margin-bottom:15px;color:#808080;">Şifremi Sıfırlama Kodum:</p>
                <div class="reset-code">{{hash}}</div>
                <p class="expires">Bu kod {{expires}} tarihine kadar geçerlidir.</p>
            </td>
        </tr>
        <tr>
            <td style="padding:20px;color:#808080;font-size:16px;font-weight:100">
                {{text4}}
            </td>
        </tr>
    </table>
</body>

</html>